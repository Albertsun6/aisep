# 契约包异构评审辩论记录(2026-06-11)

- **评审者**:cursor-agent CLI(`-p --mode ask`,未指定 `--model`);自报身份 **GPT-5.5**(自报,工具未提供可观测模型字段——按契约 05 注明)。
- **输入**:契约 01~10 + README + ADR 修订 diff(`/tmp/contracts_review_input.txt`,23KB);完整评审输出存档于本文件末尾引用的 commit。
- **verdict**:需修订后生效 → 本次落改后生效。
- **裁决人**:Claude(Fable 5)+ 用户授权的 goal 模式;被驳倒即改,如下表。

## 辩论矩阵(33 条 → 接受 24 / 部分 7 / 反驳 2)

| 契约 | 意见 | 裁决 | 落改 |
|---|---|---|---|
| 01 | "不可绕过"过满:admin 可 bypass,需 enforce_admins 等显式配置 | **接受** | 增机器可验证配置清单(enforce_admins、required check 名 `gates`、Require review from Code Owners) |
| 01 | CODEOWNERS 本身不强制 | **接受** | 并入配置清单 |
| 01 | 探针需钉死 required check 名/触发方式 | **接受** | 探针定义写明 check 名与语义 |
| 01 | "hooks"歧义 | **接受** | 限定"本地 git hooks / 会话 hooks" |
| 02 | tag 可移动,需记 SHA | **接受** | 契约内记 immutable SHA;远端建后设 protected tag |
| 02 | "任何时点双绿"不可执行 | **部分** | 改"在记录的环境(Python/依赖版本)下必须绿";环境记录附在契约 |
| 02 | 冻结时间语义混乱 | **接受** | 写明:约定层即刻生效,远端强制 M4 起 |
| 02 | 异构评审者范围窄 | **接受** | cursor-agent 或 codex;皆缺→人审 |
| 02/06 | feature↔代码映射规则缺失 | **接受** | 声明制:`--feature`/`AIFORGE_FEATURE`;src/tests 改动无声明 → 1 |
| 03 | import 失败兜底实现边界 | **接受** | 极薄 `__main__.py`(顶层零复杂 import) |
| 03 | 外部退出码(130/137/124) | **接受** | 改"受控路径只产生 0/1/2/3;其他非零消费方按 infra_error 处理" |
| 03 | receipt 解析失败需可读诊断 | **接受** | 写入契约 03/06 |
| 03 | "每命令四断言"不自然 | **接受** | 改"统一 result mapper,每语义至少一条断言" |
| 04 | grep 局限/白名单不可判定 | **部分** | 钉死方式升级:token grep(范围=harness/cli/__main__ 源码)+无网络无 key 行为测试+运行期零三方依赖审计;承认覆盖边界 |
| 04 | 计费日期移出契约 | **接受** | 理由改引 ADR-009 |
| 05 | "异构"定义不精确 | **接受** | 改"提供商/模型族 ≠ 被审产物作者模型族,可留痕;可观测字段优先,自报为辅" |
| 05 | 经验案例不当规则依据 | **部分** | 改为"同质评审系统性共享盲区"为依据;案例降为背景注 |
| 05 | skill 文案当钉死偏弱 | **接受** | 钉死改"M1 CLI 结构性无自审通道 + 流程审计";skill 文案标注为实现位点非强制 |
| 06 | actor 过载 | **接受** | schema 拆 `ack` / `reviewer` 两字段 |
| 06 | 缺工具版本/argv/dirty | **接受** | 增 `aiforge_version`、`argv`、`git_dirty`、`run_id` |
| 06 | 覆盖写 vs append-only | **部分** | 保留覆盖 + `run_id`;权威消费在 CI(已提交状态);append-only 显式推迟 |
| 06 | 恶意坏 receipt 变 3 需说明 | **接受** | 写明"3 同样阻断,仅诊断含义不同" |
| 07 | TTY 非人类证明 | **接受** | 改"审计信号/反自动化摩擦,非身份认证" |
| 07 | $USER 不可靠 | **接受** | 记 user+uid+hostname+tty,标注非强身份 |
| 07 | 非 tty → 3 还是 1 | **反驳(保留 3)** | 1 会与"门禁拒绝(内容问题)"混淆;3 = "审计环境不可满足"已按其要求写明说明 |
| 07 | CI 检测不应只靠 env | **接受** | workflow 显式传 `--ci`;env `CI` 仅辅助;**两者只用于收紧(忽略 ack),伪造无放宽收益** |
| 07 | deny 不当安全依据 | **接受** | 已标护栏,措辞再强化 |
| 08 | "任何状态"过强 | **接受** | 改"**权威**状态" |
| 08 | feature lifecycle 缺失 | **部分** | 最小 tombstone:spec.md 头部 `status: active|abandoned`;拆分/合并记 ADR;复杂生命周期显式推迟 |
| 08 | agent JSON 落盘未定 | **接受** | 定 `specs/<id>/outputs/<role>.json`(内含 schema_version) |
| 08 | 引用外部方案章节 | **接受** | 工具白名单表内联进契约 08 |
| 08 | 缺 agents/*.md 机器检查 | **部分** | 登记为 M2 落地项(测试/lint),契约注明随 M2 生效 |
| 09 | 未知字段须忽略 | **接受** | 写明"消费者忽略未知字段;语义变化才升版" |
| 09 | 整数版本偏粗 | **反驳(保留)** | v1 整数 + 未知字段忽略已覆盖当前演进;需要时再细化(YAGNI,原则②) |
| 09 | hash 路径边界 | **接受** | 相对 repo root POSIX 路径;不跟随 symlink(symlink 输入→3);目录不 hash;文件缺失走 gate 语义 |
| 09 | $USER 即 PII | **接受** | 定义允许的最小身份字段(user/uid/hostname) |
| 09 | 指纹前 8 位仍有险 | **接受** | secret 一律不存指纹,记"种类+定位提示" |
| 09 | break-glass 非机器强制 | **接受** | 标"治理契约(流程),非机器钉死" |
| 10 | 路径应抽象宿主 | **部分** | 原则抽象为"各宿主配置显式";Claude 路径为当前实现示例 |
| 10 | fail-closed 限定自动化调用 | **接受** | 写明限定 CLI/driver/自动化 |
| 10 | requested/reported 区分 | **接受** | 台账字段拆 requested_model/reported_model/observed_source |
| 10 | 厂商事实移出规范正文 | **接受** | 移至 `vendor-facts.md`(非规范附录) |

> 注:条目数按落改粒度合并计;两条反驳均附理由,cursor 若有后续异议在下轮评审提出。

---

## 附:M1 代码评审辩论记录(同日,同评审者配置,自报 GPT-5.5)

输入 = M1 commit `1851bcf` 完整 diff + 契约 03/04/06/07 摘要;verdict = **需修后合入** → 已落改(见后续 commit)。

| 严重度 | 意见 | 裁决 | 落改 |
|---|---|---|---|
| Blocker | feature_id 无校验 → receipt 路径穿越 | **接受** | `^[a-z0-9_][a-z0-9._-]{0,99}$` 校验,违规 → 3;探针测试 `--feature ../evil` |
| Blocker | 手写"hash 正确"receipt 可过链校验 | **接受(分层)** | 本地加元数据校验(gate/feature_id/exit_code/inputs 路径);权威 = CI 重跑上游 gate(契约 06 第 4 条) |
| Blocker | 链校验/承诺内容 = worktree,提交内容 = index(TOCTOU) | **接受** | spec hash 改为比对 `git show :path`(index 版本);spec 不在 index → 1;正反两条测试 |
| Blocker | `SystemExit("msg")` 被映射 0(fail-open) | **接受** | 非 int → 3;None → 0;测试钉死 |
| High | symlink 检查不完整(trace 先读后查/receipt 写路径) | **接受** | 统一 `_read_no_symlink`;write/load_receipt 全路径组件查 symlink |
| High | git diff 可触发外部 diff driver | **接受** | 所有 diff 调用加 `-c diff.external= --no-ext-diff --no-textconv` |
| High | cli 顶层 import 非 gate 模块,污染契约 04 边界 | **接受** | demo/eval/dashboard 依赖全部 lazy import |
| Medium | --ack-human 只在 needs_human 才查 tty | **接受** | 传了就查(非 CI),clean diff 也 3 |
| Medium | schema_version=0/低版本被默认接受 | **接受** | 只接受 ==1,其余 3 |
| Medium | ruff 自身错误被当内容拒绝 | **接受** | rc 0/1/≥2 → 过/1/3 分流 |
| Medium | git 路径按换行/tab 解析,特殊文件名误判 | **接受** | `--name-only -z`/`--numstat -z` NUL 解析(含 rename 记录) |
| Medium | lint 检查 worktree 非 staged 快照 | **部分** | 保留 worktree lint(反馈层)+ 诚实标注;权威 = CI 对已提交树重跑 make lint |
| Low | receipt argv 缺 --feature | **接受** | argv 补全 |

验证:落改后 68 测试绿(含 8 条新增钉死测试)、ruff 0 错、2 架构契约 kept。
